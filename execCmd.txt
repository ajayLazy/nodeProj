cd /home/ubuntu/nodeProj
docker build -t serverr .

# Stop any old containers with this name (optional)
docker stop serverr || true
docker rm serverr || true

# Run the new Docker container
docker run -d -p 3005:3005 --name serverr serverr